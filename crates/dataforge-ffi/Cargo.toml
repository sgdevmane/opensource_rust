[package]
name = "dataforge-ffi"
version = "0.1.0"
edition = "2021"
description = "C ABI foreign function interface for DataForge"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
dataforge-core = { path = "../dataforge-core" }
tracing = { workspace = true }

[build-dependencies]
